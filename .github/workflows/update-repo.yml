name: Update repo with latest commit

on:
  push:
    branches:
      - main

jobs:
  update-repo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Push changes
        run: |
          git config --global user.email "Cattn@mathstudy.dev"
          git config --global user.name "Logan"
          git pull
          git push
